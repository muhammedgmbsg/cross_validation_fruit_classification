# cross_validation_fruit_classification
10 different fruit classes were classified using a deep learning-based model. The model performance was evaluated with 10-Fold Cross Validation, achieving over 80% accuracy on specific data subsets.
